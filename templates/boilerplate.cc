#include <cstdio>

#define CF_MULTI_CASES

void solve();

int main() {
#ifdef CF_MULTI_CASES
    int cases;
    scanf("%d", &cases);
    while (cases--) {
        solve();
    }
#else
    solve();
#endif
    return 0;
}


void solve() {
}
